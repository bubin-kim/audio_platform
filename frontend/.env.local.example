# 프론트엔드 환경변수 템플릿. 복사해서 frontend/.env.local 로 쓴다.
# 백엔드 API 주소 (api prefix 포함)
NEXT_PUBLIC_API_URL=http://localhost:8100/api

# 배포 환경에서만 true — 토큰 쿠키 없으면 /login으로 보낸다 (docs/13 §6)
NEXT_PUBLIC_REQUIRE_AUTH=false
